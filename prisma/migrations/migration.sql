CREATE TABLE `stagewood`.`new_table` (
  `id` INT NOT NULL,
  `title` VARCHAR(45) NULL COMMENT '	',
  `startDate` DATETIME NULL,
  `endDate` DATETIME NULL,
  `photo` VARCHAR(45) NULL,
  PRIMARY KEY (`id`));

